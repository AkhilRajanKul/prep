import mysql.connector

# Establish connection
conn = mysql.connector.connect(
    host='ark-report.ctaaasqqocoi.ap-southeast-1.rds.amazonaws.com',
    user='root',
    password='ark8mQ9#tZ4!',
    database='ark-reports-local'
)

cursor = conn.cursor()

# List all tables
cursor.execute("SHOW TABLES")
tables = cursor.fetchall()
print("Tables in the database:")
for table in tables:
    print(table[0])
